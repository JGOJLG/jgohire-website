import Link from"next/link";import{redirect}from"next/navigation";import{createClient}from"@/lib/supabase/server";
export default async function Portal(){const s=await createClient(),{data:{user}}=await s.auth.getUser();if(!user)redirect("/login?next=/portal");await s.rpc("claim_jgo_client_portal");const[{data:client},{data:guide}]=await Promise.all([s.from("clients").select("id,name").eq("portal_user_id",user.id).maybeSingle(),s.from("member_entitlements").select("id").eq("user_id",user.id).eq("entitlement","linkedin_guide").maybeSingle()]);const first=client?.name?.split(/\s+/)[0]||user.email?.split("@")[0]||"there";return <main className="min-h-screen bg-[#f5f1ea] px-5 py-10 text-[#223028]"><div className="mx-auto max-w-6xl"><header className="flex flex-wrap items-center justify-between gap-4 border-b border-[#ddd8ce] pb-6"><Link href="/" className="font-serif text-2xl">JGO HIRE</Link><div className="flex items-center gap-4 text-sm"><span className="hidden text-[#667168] sm:inline">{user.email}</span><form action="/auth/signout" method="post"><button className="font-bold text-[#4d6247]">Log Out</button></form></div></header><section className="py-10"><p className="text-xs font-bold uppercase tracking-[.18em] text-[#637a5b]">Member Portal</p><h1 className="mt-2 font-serif text-4xl sm:text-5xl">Welcome, {first}.</h1><p className="mt-3 max-w-2xl text-[#667168]">Your JGO Hire tools, resources, and purchases live here. You&apos;ll only see the areas included with your account.</p><div className="mt-9 grid gap-5 md:grid-cols-2">{client?<Link href="/client-portal" className="rounded-3xl border border-[#dcd8cf] bg-white p-7 shadow-sm"><p className="text-xs font-bold uppercase tracking-wide text-[#637a5b]">Client Access</p><h2 className="mt-2 font-serif text-3xl">My Career Portal</h2><p className="mt-3 leading-6 text-[#667168]">Your documents, JGO resources, and personal job tracker.</p><p className="mt-6 font-bold text-[#4d6247]">Open Client Portal →</p></Link>:null}{guide?<Link href="/course" className="rounded-3xl border border-[#dcd8cf] bg-[#eef2eb] p-7 shadow-sm"><p className="text-xs font-bold uppercase tracking-wide text-[#637a5b]">Purchased Guide</p><h2 className="mt-2 font-serif text-3xl">LinkedIn Optimization Guide</h2><p className="mt-3 leading-6 text-[#667168]">Continue your complete LinkedIn profile optimization course.</p><p className="mt-6 font-bold text-[#4d6247]">Continue Guide →</p></Link>:null}</div>{!client&&!guide?<div className="mt-8 rounded-2xl border bg-white p-7"><h2 className="font-serif text-2xl">Your account is ready.</h2><p className="mt-2 text-[#667168]">There are no products or client services connected to this account yet. If you expected access, contact JGO Hire.</p></div>:null}</section></div></main>}

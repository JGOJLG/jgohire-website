import { redirect } from "next/navigation";
import { createClient } from "@/lib/supabase/server";
export default async function Resources(){const supabase=await createClient();const{data:{user}}=await supabase.auth.getUser();if(!user)redirect("/client-access?next=/client-portal/resources");const{data}=await supabase.from("client_portal_resources").select("*").order("sort_order");return <main className="min-h-screen bg-[#f5f1ea] px-5 py-8 text-[#223028]"><div className="mx-auto max-w-5xl"><a href="/client-portal" className="text-sm font-semibold text-[#637a5b]">← Portal</a><h1 className="mt-3 font-serif text-4xl">Resources</h1><p className="mt-2 text-[#667168]">Your JGO Hire guides, templates, and job-search tools.</p><div className="mt-7 grid gap-4 md:grid-cols-2">{data?.map(r=><a key={r.id} href={r.url} target="_blank" rel="noreferrer" className="rounded-2xl border border-[#e0ddd5] bg-white p-6 shadow-sm"><p className="text-xs font-bold uppercase tracking-wide text-[#637a5b]">{r.category}</p><h2 className="mt-2 font-serif text-xl">{r.title}</h2>{r.description?<p className="mt-2 text-sm leading-6 text-[#667168]">{r.description}</p>:null}<p className="mt-4 text-sm font-bold text-[#4d6247]">Open resource →</p></a>)}{!data?.length?<div className="rounded-2xl border border-[#e0ddd5] bg-white p-8 text-[#667168]">Resources shared with you will appear here.</div>:null}</div></div></main>}

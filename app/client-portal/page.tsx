import Link from"next/link";import{redirect}from"next/navigation";import{createClient}from"@/lib/supabase/server";
export const dynamic="force-dynamic";
export default async function Page(){const s=await createClient();const{data:{user}}=await s.auth.getUser();if(!user)redirect("/client-portal/login");await s.rpc("claim_jgo_client_portal");const{data:client}=await s.from("clients").select("id,name,email,next_step,due_date").eq("portal_user_id",user.id).order("id",{ascending:false}).limit(1).maybeSingle();if(!client)return <main className="cp-main"><div className="cp-shell"><div className="cp-card"><p className="cp-eyebrow">JGO Hire Member</p><h1 className="cp-title">Your account is active.</h1><p className="cp-subtitle">This login is not connected to a client profile yet. If you purchased the LinkedIn Guide, you can continue there. If you are a coaching client, contact JGO Hire so we can connect your portal.</p><div style={{marginTop:20,display:"flex",gap:10,flexWrap:"wrap"}}><Link className="cp-button" href="/course">Open LinkedIn Guide</Link><Link className="cp-button secondary" href="/contact">Contact JGO Hire</Link></div></div></div></main>;const[{data:jobs},{data:files}]=await Promise.all([s.from("client_job_applications").select("id,status,company,job_title,next_step,next_step_date,updated_at").eq("user_id",user.id).order("updated_at",{ascending:false}),s.from("client_files").select("id").eq("client_id",client.id).eq("visible_in_portal",true).eq("archived",false)]);const all=jobs??[],submitted=all.filter(j=>!["Open / Interested","Rejected","Withdrawn","Closed"].includes(j.status)).length,interviewing=all.filter(j=>["Recruiter Screen","Interviewing","Final Interview"].includes(j.status)).length,offers=all.filter(j=>j.status==="Offer").length,first=(client.name||"there").split(" ")[0];return <main className="cp-main"><div className="cp-shell"><p className="cp-eyebrow">Your JGO workspace</p><h1 className="cp-title">Welcome back, {first}.</h1><p className="cp-subtitle">Your job search, documents, resources, and JGO support all live here.</p><section className="cp-section cp-grid cp-grid-4"><div className="cp-card cp-stat"><strong>{submitted}</strong><span>Applications out</span></div><div className="cp-card cp-stat"><strong>{interviewing}</strong><span>Interviewing</span></div><div className="cp-card cp-stat"><strong>{offers}</strong><span>Offers</span></div><div className="cp-card cp-stat"><strong>{files?.length??0}</strong><span>Documents</span></div></section><section className="cp-section"><div className="cp-section-head"><div><p className="cp-eyebrow">Quick access</p><h2>What do you need today?</h2></div></div><div className="cp-grid cp-grid-3"><Link className="cp-card cp-action" href="/client-portal/jobs"><div><div className="cp-action-icon">JT</div><h3 style={{marginTop:18}}>Job Tracker</h3><p className="cp-muted">Track every role, recruiter, follow-up, interview, and offer.</p></div><span className="cp-action-arrow">&gt;</span></Link><Link className="cp-card cp-action" href="/client-portal/files"><div><div className="cp-action-icon">DOC</div><h3 style={{marginTop:18}}>Documents</h3><p className="cp-muted">Access resumes, cover letters, and files shared by JGO Hire.</p></div><span className="cp-action-arrow">&gt;</span></Link><Link className="cp-card cp-action" href="/client-portal/resources"><div><div className="cp-action-icon">JGO</div><h3 style={{marginTop:18}}>Client Resources</h3><p className="cp-muted">Interview Ready, CORE Framework, and resources selected for you.</p></div><span className="cp-action-arrow">&gt;</span></Link></div></section><section className="cp-section cp-grid cp-grid-2"><div className="cp-card"><p className="cp-eyebrow">Next step</p><h2 style={{marginTop:8}}>{client.next_step||"Keep your search moving"}</h2><p className="cp-muted">{client.due_date?`Target date: ${client.due_date}`:"Use your tracker to keep follow-ups and interviews organized."}</p></div><div className="cp-card"><p className="cp-eyebrow">Recent activity</p>{all.length?all.slice(0,4).map(j=><div key={j.id} style={{padding:"10px 0",borderBottom:"1px solid #eeeae3"}}><strong style={{fontSize:13}}>{j.company}</strong><div className="cp-muted">{j.job_title} - {j.status}</div></div>):<p className="cp-muted">No jobs added yet. Start with the Job Tracker.</p>}</div></section></div></main>}

"use client";
import{useState}from"react";import{useRouter}from"next/navigation";import{createClient}from"@/lib/supabase/client";
export default function SignupForm({email}:{email:string}){const router=useRouter();const[password,setPassword]=useState(""),[confirm,setConfirm]=useState(""),[busy,setBusy]=useState(false),[error,setError]=useState(""),[sent,setSent]=useState(false);async function submit(e:React.FormEvent){e.preventDefault();setError("");if(password.length<8){setError("Use at least 8 characters for your password.");return}if(password!==confirm){setError("The passwords do not match.");return}setBusy(true);const s=createClient();const redirectTo=`${window.location.origin}/auth/callback?next=/client-portal`;const{data,error:signError}=await s.auth.signUp({email:email.trim().toLowerCase(),password,options:{emailRedirectTo:redirectTo}});if(signError){const msg=signError.message.toLowerCase();setError(msg.includes("already")?"An account already exists for this email. Use Log in instead.":signError.message);setBusy(false);return}if(data.session){await s.rpc("claim_jgo_client_portal");router.replace("/client-portal");router.refresh();return}setSent(true);setBusy(false)}if(sent)return <div className="cp-alert" style={{background:"#edf2e9",color:"#4d6247"}}>Check your email and confirm your account. After confirmation, you will go directly to your JGO Hire Client Portal.</div>;return <form onSubmit={submit} className="cp-form"><label className="cp-label">Email<input className="cp-input" value={email} readOnly type="email"/></label><label className="cp-label">Create password<input className="cp-input" value={password} onChange={e=>setPassword(e.target.value)} type="password" minLength={8} required/></label><label className="cp-label">Confirm password<input className="cp-input" value={confirm} onChange={e=>setConfirm(e.target.value)} type="password" minLength={8} required/></label>{error?<div className="cp-alert">{error}</div>:null}<button className="cp-button" disabled={busy}>{busy?"Creating account...":"Create my portal account"}</button></form>}

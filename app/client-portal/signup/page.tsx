import Link from"next/link";import SignupForm from"./SignupForm";export default async function Page({searchParams}:{searchParams:Promise<{email?:string}>}){const{email=""}=await searchParams;return <main className="min-h-screen bg-[#f5f1ea] px-5 py-12 text-[#223028]"><div className="mx-auto max-w-md rounded-3xl border bg-white p-8 shadow-sm"><p className="text-xs font-bold uppercase tracking-[.18em] text-[#637a5b]">JGO Hire Client Portal</p><h1 className="mt-3 font-serif text-3xl">Create your account</h1><p className="mt-3 text-sm leading-6 text-[#667168]">You&apos;ve been invited to your private JGO Hire Client Portal. Create a password below using the email your invitation was sent to.</p>{email?<SignupForm email={email}/>:<div className="mt-6 rounded-xl bg-[#fbefed] p-4 text-sm">Open the Client Portal from your invitation email so we can verify the invited email address.</div>}<p className="mt-6 text-center text-sm text-[#667168]">Already created your account? <Link href={`/login?next=/client-portal${email?`&email=${encodeURIComponent(email)}`:""}`} className="font-bold text-[#4d6247]">Log in</Link></p></div></main>}

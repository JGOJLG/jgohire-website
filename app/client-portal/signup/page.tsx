import Link from"next/link";import SignupForm from"./SignupForm";import"../portal.css";export default async function Page({searchParams}:{searchParams:Promise<{email?:string}>}){const{email=""}=await searchParams;return <main className="cp-login"><div className="cp-auth-card"><p className="cp-eyebrow">JGO Hire Client Portal</p><h1>Create your account.</h1><p>Use the email address your JGO Hire invitation was sent to. Once confirmed, your account connects to your client profile automatically.</p>{email?<SignupForm email={email}/>:<div className="cp-alert">Open the portal from your invitation email so we can use the invited email address.</div>}<p style={{marginTop:20,textAlign:"center",fontSize:13}}>Already registered? <Link className="cp-link" href={`/client-portal/login${email?`?email=${encodeURIComponent(email)}`:""}`}>Log in</Link></p></div></main>}

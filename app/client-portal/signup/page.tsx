import Link from"next/link";import SignupForm from"./SignupForm";import"../portal.css";export default async function Page({searchParams}:{searchParams:Promise<{email?:string}>}){const{email=""}=await searchParams;return <main className="cp-login" style={{minHeight:"calc(100vh - 76px)",display:"grid",placeItems:"center",padding:"36px 18px",background:"#f6f2eb"}}><div className="cp-auth-card" style={{width:"min(100%,520px)",background:"#fff",border:"1px solid #dfdbd3",borderRadius:24,padding:"clamp(24px,5vw,38px)",boxShadow:"0 18px 50px rgba(34,48,40,.08)"}}><p className="cp-eyebrow">JGO Hire Account</p><h1 style={{margin:"10px 0 0",fontFamily:"Georgia,serif",fontSize:"clamp(30px,7vw,42px)",fontWeight:500,color:"#223028"}}>Create your password.</h1><p style={{margin:"12px 0 24px",color:"#68746b",fontSize:14,lineHeight:1.6}}>Create one secure password for your JGO Hire account. You’ll use this same login for anything you have access to, including the LinkedIn Optimization Guide and the Client Portal.</p>{email?<SignupForm email={email}/>:<div className="cp-alert">Open this page from your JGO Hire invitation or purchase email so we can use the correct email address.</div>}<p style={{marginTop:20,textAlign:"center",fontSize:13}}>Already registered? <Link className="cp-link" href={`/client-portal/login${email?`?email=${encodeURIComponent(email)}`:""}`}>Log in</Link></p></div></main>}

import Link from"next/link";import LoginForm from"./LoginForm";import"../portal.css";export default async function Page({searchParams}:{searchParams:Promise<{email?:string}>}){const{email=""}=await searchParams;return <main className="cp-login"><div className="cp-auth-card"><p className="cp-eyebrow">JGO Hire Client Portal</p><h1>Welcome back.</h1><p>Your documents, job tracker, resources, and next steps are all in one place.</p><LoginForm email={email}/><p style={{marginTop:20,textAlign:"center",fontSize:13}}>New client? <Link className="cp-link" href={`/client-portal/signup${email?`?email=${encodeURIComponent(email)}`:""}`}>Create your account</Link></p></div></main>}

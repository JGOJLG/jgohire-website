import { redirect } from "next/navigation";
import { createClient } from "@/lib/supabase/server";
export default async function Files(){const supabase=await createClient();const{data:{user}}=await supabase.auth.getUser();if(!user)redirect("/client-access?next=/client-portal/files");const{data:client}=await supabase.from("clients").select("id").eq("portal_user_id",user.id).maybeSingle();const{data:files}=client?await supabase.from("client_files").select("id,file_name,file_path,file_type,uploaded_at,category").eq("client_id",client.id).eq("archived",false).order("uploaded_at",{ascending:false}):{data:[]};return <main className="min-h-screen bg-[#f5f1ea] px-5 py-8 text-[#223028]"><div className="mx-auto max-w-5xl"><a href="/client-portal" className="text-sm font-semibold text-[#637a5b]">← Portal</a><h1 className="mt-3 font-serif text-4xl">My Documents</h1><p className="mt-2 text-[#667168]">Your resume and files shared by JGO Hire.</p><div className="mt-7 space-y-3">{files?.map(f=><div key={f.id} className="flex items-center justify-between gap-4 rounded-2xl border border-[#e0ddd5] bg-white p-5"><div><p className="font-semibold">{f.file_name}</p><p className="mt-1 text-xs text-[#7a847c]">{f.category||f.file_type||"Document"}</p></div><a href={f.file_path} target="_blank" rel="noreferrer" className="rounded-xl border border-[#4d6247] px-4 py-2 text-sm font-bold text-[#4d6247]">Open</a></div>)}{!files?.length?<div className="rounded-2xl border border-[#e0ddd5] bg-white p-8 text-[#667168]">Your JGO Hire documents will appear here as they are shared with you.</div>:null}</div></div></main>}
